version: 0.2

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - npm run build || echo "No build script, skipping..."
  post_build:
    commands:
      - zip -r app.zip .

artifacts:
  files:
    - app.zip
